# v3 single-origin build.
# Empty VITE_API_URL makes the app call the API with relative paths (/api/...),
# so it works against whatever origin/port the backend is served from.
# Do NOT set this to a full URL unless you host the frontend separately.
VITE_API_URL=
