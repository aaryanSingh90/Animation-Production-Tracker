# Always install devDependencies, even when NODE_ENV=production.
# Render sets NODE_ENV=production by default, which would otherwise cause
# `npm install` to skip typescript, @types/*, prisma, tsx — all of which
# we need at build time to compile and seed.
production=false
